import {useRouter} from "vue-router";import {computed, unref} from "vue";export const useQueryParameters = () => {    const router = useRouter();    const queryParams = computed(() => unref(router.currentRoute).query);    const first_name = computed(() => unref(queryParams).n1);    const second_name = computed(() => unref(queryParams).n2);    const gender = computed(() => unref(queryParams).gn);    const isOnePerson = computed(() => !unref(second_name) && unref(first_name));    return {        isOnePerson,        gender,        first_name,        second_name,    };}